export default function iterateThroughObject(reportWithIterator) {
  let employeees = [];
  for (let member of reportWithIterator) {
    for (let dep of member) {
      let str = part + ' | ';
      employees.push(part);
    }
  }
}
